name: Doxygen

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  pull_request:
  workflow_dispatch:
    inputs:
      build_pdf:
        description: "Build the LaTeX PDF artifact"
        required: false
        default: false
        type: boolean

permissions:
  contents: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  docs:
    name: Build documentation
    if: ${{ github.event_name != 'push' || github.event.head_commit == null || !contains(github.event.head_commit.message, '[skip ci]') }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            doxygen \
            graphviz

      - name: Install LaTeX dependencies
        if: ${{ (github.event_name == 'push' && github.ref == 'refs/heads/main') || startsWith(github.ref, 'refs/tags/') || (github.event_name == 'workflow_dispatch' && inputs.build_pdf) }}
        run: |
          sudo apt-get install -y \
            texlive-fonts-recommended \
            texlive-latex-extra \
            texlive-latex-recommended \
            texlive-xetex

      - name: Verify Doxygen installation
        run: |
          doxygen --version
          dot -V

      - name: Clean previous generated docs
        run: |
          rm -rf docs/html docs/latex .doxygen/doxygen_warnings.log

      - name: Generate current Doxygen docs
        run: |
          cd .doxygen && doxygen Doxyfile

      - name: Organize LaTeX build tree
        run: |
          test -f docs/latex/refman.tex
          mkdir -p docs/latex/build
          find docs/latex -mindepth 1 -maxdepth 1 ! -name build -exec mv -t docs/latex/build {} +
          cp docs/latex/build/refman.tex docs/latex/refman.tex

      - name: Create .nojekyll files for GitHub Pages
        run: |
          touch docs/.nojekyll
          touch docs/html/.nojekyll

      - name: Verify deploy entrypoint
        run: |
          test -f docs/html/index.html

      - name: Build PDF from LaTeX
        if: ${{ (github.event_name == 'push' && github.ref == 'refs/heads/main') || startsWith(github.ref, 'refs/tags/') || (github.event_name == 'workflow_dispatch' && inputs.build_pdf) }}
        run: |
          test -f docs/latex/build/Makefile
          make -C docs/latex/build
          cp docs/latex/build/refman.pdf docs/latex/refman.pdf

      - name: Commit generated documentation
        if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs
          if git diff --cached --quiet; then
            echo "No generated documentation changes to commit."
          else
            git commit -m "docs: update Doxygen documentation [skip ci]"
            git push origin HEAD:${{ github.ref_name }}
          fi

      - name: Summarize Doxygen warnings
        if: always()
        run: |
          if [ -s .doxygen/doxygen_warnings.log ]; then
            echo "Doxygen warnings:"
            wc -l .doxygen/doxygen_warnings.log
            sed -n '1,120p' .doxygen/doxygen_warnings.log
          else
            echo "No Doxygen warnings log was generated."
          fi

      - name: Upload HTML documentation
        uses: actions/upload-artifact@v4
        with:
          name: doxygen-html
          path: docs/html
          if-no-files-found: error

      - name: Upload warning log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: doxygen-warnings
          path: .doxygen/doxygen_warnings.log
          if-no-files-found: ignore

      - name: Upload PDF documentation
        if: ${{ startsWith(github.ref, 'refs/tags/') || (github.event_name == 'workflow_dispatch' && inputs.build_pdf) }}
        uses: actions/upload-artifact@v4
        with:
          name: doxygen-pdf
          path: docs/latex/refman.pdf
          if-no-files-found: error
